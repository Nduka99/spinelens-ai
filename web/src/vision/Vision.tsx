/**
 * Vision — concept-render gallery (artist's impressions).
 * Step 0: placeholder. Step 8 adds the curated, optimised render lightbox/carousel
 * (each captioned "Concept visual — artist's impression").
 */

export function Vision() {
  return (
    <section className="view-panel" aria-label="The vision gallery">
      <h2 className="view-panel__title">The Vision</h2>
      <p className="view-panel__lead">
        Concept visuals of the spine, crossing, wayfinders and pavilion — artist's impressions.
      </p>
      <p className="view-panel__note">The render gallery arrives in a later step.</p>
    </section>
  );
}
